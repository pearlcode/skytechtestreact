import React from 'react';

export default (props) => {

    // no time to implment pagination
    // would ideally set state in parent component

    return <div className="pagination">paginator</div>
}